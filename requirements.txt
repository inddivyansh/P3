# ==============================================================================
# AI-Powered Defence & Geopolitical Intelligence Platform
# Master Requirements Specification (Unified Edition)
# Supports Python 3.10, 3.11, and 3.12
# ==============================================================================

# ── Core Ingestion & Web Scraping ─────────────────────────────────────────────
feedparser>=6.0.10          # RSS / Atom feed parsing (Stage 1 + P2 RSSConnector)
requests>=2.31.0            # HTTP client for external connectors & article fetching
trafilatura>=1.6.0          # Full-text content extraction (Stage 2 article fetcher)
beautifulsoup4>=4.12.0      # HTML parser fallback for text extraction
pyyaml>=6.0.0               # YAML config parsing (feeds.yaml, taxonomy.yaml, sources.yaml)

# ── D-P2-21 Multi-Source Ingestion Framework ──────────────────────────────────
sqlalchemy>=2.0.0           # SQLAlchemy 2.0 Core for P2 SQLiteStore unified store
python-json-logger>=2.0.7   # Structured JSON logging for P2 connector audit trails
tenacity>=8.2.0             # Exponential-backoff retry logic (@transient_retry)

# ── Data Validation & Schemas ─────────────────────────────────────────────────
pydantic>=2.0.0             # Canonical UnifiedRecord model validation & FastAPI schemas
pydantic-settings>=2.2.0    # Application settings management with .env support

# ── Cognitive NLP, Machine Learning & Embeddings ──────────────────────────────
sentence-transformers>=2.3.0    # Stage 4: Semantic embeddings for deduplication & search
transformers>=4.36.0            # Stage 5 & 9: Zero-shot classification & sentiment
torch>=2.1.0                    # PyTorch tensor computation backend for transformers
scikit-learn>=1.3.0             # Vector cosine similarity, clustering utilities
pandas>=2.1.0                   # Tabular data manipulation (GDELT CSV processing)
numpy>=1.26.0                   # Numerical operations & array manipulation
spacy>=3.7.0                    # Stage 6: Military NER + Location Intelligence
                                # Post-install: python -m spacy download en_core_web_sm

# ── Generative AI (Google Gemini) ─────────────────────────────────────────────
google-generativeai>=0.8.0      # Stage 7/8 & Chat: Executive summaries, threats, RAG

# ── Vector Database & Semantic Retrieval ──────────────────────────────────────
chromadb>=0.5.0                 # Stage 10 & RAG: Persistent embedded vector store

# ── Backend API & ASGI Server ─────────────────────────────────────────────────
fastapi>=0.110.0                # High-performance async REST API framework
uvicorn[standard]>=0.28.0       # ASGI production server with WebSocket support
python-multipart>=0.0.9         # Multipart form data & file upload parsing
python-dotenv>=1.0.0            # Environment variable loading from .env
httpx>=0.27.0                   # HTTP client for testing FastAPI endpoints

# ── Quality Assurance & Automated Testing ─────────────────────────────────────
pytest>=8.0.0                   # Automated test framework (57 test cases)
pytest-cov>=4.1.0               # Test coverage analysis & metrics reporting
pytest-mock>=3.12.0             # Mocking fixtures for unit & integration tests
